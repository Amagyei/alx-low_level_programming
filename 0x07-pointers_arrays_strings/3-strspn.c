#include <string.h>
/**
 * _strspn - assign bytes of length b to string s
 * Description : 'same thins '
 * @s : the string
 * @accept: length of bytes
 * Return: first somethiing
 */

unsigned int _strspn(char *s, char *accept)
{
	int a = sizeof(_strspn(s, accept));
	return (a);
}

