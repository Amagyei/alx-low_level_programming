#include "main.h"
#include <stdio.h>

/**
 * _strlen - check the code
 *
 * Description : 'check the code'
 *
 * @s: A parameter to check for lowercase
 * Return: Always 0.
 */

int _strlen(char *s)
{
return(len(*s));
}
