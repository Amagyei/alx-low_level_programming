#include "main.h"
#include <stdio.h>

/**
 * _puts - check the code
 *
 * Description : 'check the code'
 *
 * @str: A parameter to check for lowercase
 * Return: Always 0.
 */

void _puts(char *str)
{
puts(str);
}
