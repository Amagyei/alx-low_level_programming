#include <stdio.h>
#include <stdlib.h>
/**
 * add - Entry point
 * @a : this thing
 * Description : 'printing whether random number is negative positive or zer'
 * @b : this thing
 * Return: Always 0 (success)
 */
int add(int a, int b)
{

return (a + b);
}
