#include <string.h>
/**
 * _memset - assign bytes of length b to string s
 * Description : 'same thins '
 * @s : the string
 * @b: length of bytes
 * @n :no of bytes
 * Return: first somethiing
 */

char *_memset(char *s, char b, unsigned int n)
{
	return (memset(s, b, n));

}

